#!/usr/bin/env bash
ROOT_DIR="$(dirname $0)/../"
source "${ROOT_DIR}/etc/settings"

mkdir -p "${INCOMING_DIR}"

###############################################################################
PACKAGES=(
  jq
  lftp
)
apt-get update
apt-get install -y ${PACKAGES[@]}
###############################################################################
